public class MasterFunction {
    public static String setTitleCase(String input) {
        var toLower = input.toLowerCase();
        //to do
        //convert first character to upper case
        return toLower;
    }

    public static boolean checkIsValidEmail(String input) {
        //to do
        //check if input email is valid
        return true;
    }
}